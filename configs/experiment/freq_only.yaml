# @package _global_
# Control: frequency split only, same backbone for both streams, no role/task bias.
# Tests: is the input frequency bias alone sufficient for asymmetry?
model:
  global_:
    backbone: resnet50
    pretrained: true
